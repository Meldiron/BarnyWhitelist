package sk.meldiron.barlywhitelist.managers;

public class GUIManager {
    /*
    public static SmartInventory inv = SmartInventory.builder()
            .manager(Main.i.invManager)
            .provider(new TemplateGUI())
            .size(4, 9)
            .title(Utils.color("&cTemplate GUI :)"))
            .build();
    */
}
